import { Shield } from "lucide-react";

/** Shown only when the user has backend RolesRead permission. */
export const ACCESS_NAV_ITEM = {
  href: "/admin/access",
  label: "Access control",
  icon: Shield,
};
